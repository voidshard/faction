syntax = "proto3";
option go_package = "pkg/structs";

enum ErrorCode {
    OK = 0; // Or "no error"
    INTERNAL = 1;
    INVALID = 2;
    CONFLICT = 3;
    NOT_FOUND = 4;
    TIMEOUT = 5;
}

message Error {
    string Message = 1;
    ErrorCode Code = 2;
}
